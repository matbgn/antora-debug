{
  "description": "Antora Lunr Pipeline Extension example",
  "scripts": {
    "build": "set \"DOCSEARCH_ENABLED=true\" && set \"DOCSEARCH_ENGINE=lunr\" && antora --generator antora-site-generator-lunr antora-playbook.yml --clean"
  },
  "devDependencies": {
    "antora-site-generator-lunr": "^0.5.0"
  }
}
